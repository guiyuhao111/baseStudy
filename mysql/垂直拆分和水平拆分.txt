1.什么是垂直拆分和水平拆分？
	---1.垂直拆分:
	一般是在设计数据表结构时拆分
	将一张表的数据结构拆分为多张表，将经常查询的数据放在一起，不经常查询的数据放在一起，
		缺点:1.同时取多张表的数据时需要join
		     2.插入时有可能需要同时对多张表进行同时插入
		优点:查询经常查询的数据时效率高
	---2.水平拆分:
	一般是数据量比较大时对数据按一定的规则(例如用id取模)进行拆分，拆分之后多张表数据结构一样，不改变原数据库的结构
		优点:如果知道要查询的表的话查询数据很高，如果不知道要查询那几张表的话查询速率低